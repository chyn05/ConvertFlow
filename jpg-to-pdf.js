document.addEventListener('DOMContentLoaded',function(){const input=document.getElementById('jpgToPdfInput');const btn=document.getElementById('jpgToPdfButton');const dl=document.getElementById('jpgToPdfDownload');btn&&btn.addEventListener('click',async()=>{if(!input.files.length) return alert('Select images');const { jsPDF }=window.jspdf;const doc=new jsPDF({unit:'pt',format:'a4'});for(let i=0;i<input.files.length;i++){const f=input.files[i];const dataUrl=await new Promise(r=>{const fr=new FileReader();fr.onload=e=>r(e.target.result);fr.readAsDataURL(f);});const img=new Image();await new Promise(res=>{img.onload=()=>{const w=doc.internal.pageSize.getWidth();const h=doc.internal.pageSize.getHeight();doc.addImage(img,w>0?img.width:100,0, w, h); res();}; img.src=dataUrl;}); if(i<input.files.length-1) doc.addPage();}const blob=doc.output('blob');dl.href=URL.createObjectURL(blob);dl.style.display='inline-block';});});