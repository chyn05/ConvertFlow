document.addEventListener('DOMContentLoaded',function(){const btn=document.getElementById('docToPdfButton');const ta=document.getElementById('docText');const dl=document.getElementById('docToPdfDownload');btn&&btn.addEventListener('click',()=>{if(!ta.value.trim()) return alert('Paste text');const { jsPDF }=window.jspdf;const doc=new jsPDF({unit:'pt',format:'a4'});const lines=doc.splitTextToSize(ta.value,515);let y=50;lines.forEach(line=>{if(y>770){doc.addPage();y=50;}doc.text(line,40,y);y+=14;});const blob=doc.output('blob');dl.href=URL.createObjectURL(blob);dl.style.display='inline-block';});});